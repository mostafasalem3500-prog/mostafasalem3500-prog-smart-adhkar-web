let adhkar=[];let activeCategory='الكل';let activeRoutine='morning';let routineItems=[];let routineIdx=0;let routineCount=0;let tasbeeh=Number(localStorage.tasbeeh||0);let tasbeehGoal=Number(localStorage.tasbeehGoal||100);const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];
const favs=()=>new Set(JSON.parse(localStorage.favorites||'[]'));const saveFavs=s=>localStorage.favorites=JSON.stringify([...s]);
function toast(t){const el=$('#toast');el.textContent=t;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1700)}
function showView(id){$$('.view').forEach(v=>v.classList.toggle('active',v.id===id));$$('[data-nav]').forEach(b=>b.classList.toggle('active',b.dataset.nav===id));scrollTo({top:0,behavior:'smooth'});if(id==='routine')loadRoutine(activeRoutine)}
function speak(text){if(!('speechSynthesis'in window))return toast('الاستماع غير مدعوم في هذا المتصفح');speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='ar-SA';u.rate=.78;speechSynthesis.speak(u)}
function renderCard(d){const f=favs().has(d.id);return `<article class="dhikr-card"><div class="card-top"><span class="pill">${d.category}</span><button class="heart ${f?'on':''}" data-fav="${d.id}" aria-label="المفضلة">${f?'♥':'♡'}</button></div><h3>${d.title}</h3><p class="text">${d.text}</p><div class="meta"><span>التكرار: ${d.target}</span>${d.timeHint?`<span>• ${d.timeHint}</span>`:''}</div><div class="card-actions"><button class="secondary" data-speak="${d.id}">🔊 استماع</button><a class="secondary" href="${d.sourceUrl||'#'}" target="_blank" rel="noopener">↗ المصدر</a></div></article>`}
function wireCards(){$$('[data-fav]').forEach(b=>b.onclick=()=>{const s=favs();s.has(b.dataset.fav)?s.delete(b.dataset.fav):s.add(b.dataset.fav);saveFavs(s);renderAll();toast('تم تحديث المفضلة')});$$('[data-speak]').forEach(b=>b.onclick=()=>speak(adhkar.find(x=>x.id===b.dataset.speak)?.text||''))}
function renderLibrary(){let q=$('#search')?.value.trim().toLowerCase()||'';let data=adhkar.filter(d=>(activeCategory==='الكل'||d.category===activeCategory)&&(!q||[d.title,d.text,d.category,d.sourceName,(d.tags||[]).join(' ')].join(' ').toLowerCase().includes(q)));$('#libraryCards').innerHTML=data.map(renderCard).join('')||'<p>لا توجد نتائج.</p>';wireCards()}
function renderAll(){if(!adhkar.length)return;$('#featured').innerHTML=adhkar.slice(0,4).map(renderCard).join('');$('#countAdhkar').textContent=adhkar.length;$('#countCats').textContent=new Set(adhkar.map(x=>x.category)).size;$('#favCount').textContent=favs().size;renderLibrary();wireCards();updateToday()}
function initCategories(){const cats=['الكل',...new Set(adhkar.map(x=>x.category))];$('#categories').innerHTML=cats.map((c,i)=>`<button class="${i===0?'active':''}" data-cat="${c}">${c}</button>`).join('');$$('[data-cat]').forEach(b=>b.onclick=()=>{activeCategory=b.dataset.cat;$$('[data-cat]').forEach(x=>x.classList.toggle('active',x===b));renderLibrary()})}
function routineFor(type){if(type==='morning')return adhkar.filter(d=>(d.tags||[]).includes('الصباح')&&!d.id.startsWith('evening-'));if(type==='evening')return adhkar.filter(d=>(d.tags||[]).includes('المساء')&&!d.id.startsWith('morning-asbahna')&&!d.id.startsWith('morning-bika'));return adhkar.filter(d=>d.category==='أذكار بعد الصلاة')}
function loadRoutine(type){activeRoutine=type||activeRoutine;routineItems=routineFor(activeRoutine);const key='routine-'+activeRoutine;const saved=JSON.parse(localStorage.getItem(key)||'{}');routineIdx=Math.min(saved.idx||0,Math.max(0,routineItems.length-1));routineCount=saved.count||0;$('#routineTitle').textContent=activeRoutine==='morning'?'ورد الصباح':activeRoutine==='evening'?'ورد المساء':'أذكار بعد الصلاة';renderRoutine()}
function saveRoutine(){localStorage.setItem('routine-'+activeRoutine,JSON.stringify({idx:routineIdx,count:routineCount,ts:Date.now()}));updateToday()}
function renderRoutine(){const d=routineItems[routineIdx];if(!d)return;const total=routineItems.length;const pct=Math.round(((routineIdx+(routineCount/d.target))/total)*100);$('#routineIndex').textContent=`${routineIdx+1} / ${total}`;$('#routinePercent').textContent=`${Math.min(100,pct)}%`;$('#routineBar').style.width=`${Math.min(100,pct)}%`;$('#routineCategory').textContent=d.category;$('#routineItemTitle').textContent=d.title;$('#routineText').textContent=d.text;$('#routineCount').textContent=`${routineCount} / ${d.target}`;$('#routineSource').href=d.sourceUrl||'#';}
function routineAdd(n){const d=routineItems[routineIdx];routineCount=Math.max(0,Math.min(d.target,routineCount+n));if(routineCount>=d.target&&routineIdx<routineItems.length-1){routineIdx++;routineCount=0;toast('أحسنت، الذكر التالي')}else if(routineCount>=d.target&&routineIdx===routineItems.length-1){localStorage.setItem('completed-'+activeRoutine,new Date().toISOString().slice(0,10));toast('تم الورد بحمد الله')}saveRoutine();renderRoutine()}
function updateToday(){const day=new Date().toISOString().slice(0,10);let done=['morning','evening','prayer'].filter(t=>localStorage.getItem('completed-'+t)===day).length;const pct=Math.round(done/3*100);$('#todayProgress').textContent=pct+'%';$('#todayDone').textContent=done?`اكتمل ${done} من 3 أوراد`:'ابدأ أول ورد'}
function renderTasbeeh(){tasbeeh=Math.min(tasbeeh,tasbeehGoal);$('#tasbeehCount').textContent=tasbeeh;$('#tasbeehRemaining').textContent=`المتبقي ${Math.max(0,tasbeehGoal-tasbeeh)}`;$('#tasbeehBar').style.width=`${Math.min(100,tasbeeh/tasbeehGoal*100)}%`;localStorage.tasbeeh=tasbeeh;localStorage.tasbeehGoal=tasbeehGoal}
function setGoal(g){if(!g||g<1||g>100000)return;tasbeehGoal=g;tasbeeh=0;$$('[data-goal]').forEach(b=>b.classList.toggle('selected',Number(b.dataset.goal)===g));renderTasbeeh()}
function themeInit(){let t=localStorage.theme||'light';document.body.classList.toggle('dark',t==='dark');$('#themeBtn').textContent=t==='dark'?'☀':'☾'}
async function init(){adhkar=await fetch('/adhkar.json').then(r=>r.json());initCategories();renderAll();themeInit();renderTasbeeh();loadRoutine('morning');if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});}
$$('[data-nav]').forEach(b=>b.onclick=()=>showView(b.dataset.nav));$$('[data-go]').forEach(b=>b.onclick=e=>{if(b.dataset.routine)activeRoutine=b.dataset.routine;showView(b.dataset.go)});$('#search').oninput=renderLibrary;$('#themeBtn').onclick=()=>{const dark=!document.body.classList.contains('dark');localStorage.theme=dark?'dark':'light';themeInit()};$('#routinePlus').onclick=()=>routineAdd(1);$('#routineMinus').onclick=()=>routineAdd(-1);$('#routineNext').onclick=()=>{if(routineIdx<routineItems.length-1){routineIdx++;routineCount=0;saveRoutine();renderRoutine()}};$('#routinePrev').onclick=()=>{if(routineIdx>0){routineIdx--;routineCount=0;saveRoutine();renderRoutine()}};$('#routineReset').onclick=()=>{if(confirm('إعادة هذا الورد من البداية؟')){routineIdx=0;routineCount=0;localStorage.removeItem('completed-'+activeRoutine);saveRoutine();renderRoutine()}};$('#routineSpeak').onclick=()=>speak(routineItems[routineIdx]?.text||'');$$('[data-goal]').forEach(b=>b.onclick=()=>setGoal(Number(b.dataset.goal)));$('#customGoal').onchange=e=>setGoal(Number(e.target.value));$('#tapCounter').onclick=()=>{if(tasbeeh<tasbeehGoal){tasbeeh++;if(navigator.vibrate)navigator.vibrate(tasbeeh===tasbeehGoal?80:12);renderTasbeeh();if(tasbeeh===tasbeehGoal)toast('تم الهدف بحمد الله')}};$('#tasbeehReset').onclick=()=>{tasbeeh=0;renderTasbeeh()};$('#tasbeehMinus').onclick=()=>{tasbeeh=Math.max(0,tasbeeh-1);renderTasbeeh()};init();
